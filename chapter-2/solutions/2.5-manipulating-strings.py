fun_fact = "I love cat milk"

# Use Python to find out how many characters are in your fun fact. Print the result.
length = len(fun_fact)
print(length)

# Create another variable that holds your fun fact in all lower case.
fun_fact_lower_case = fun_fact.lower()

# Alter the original variable and convert it to all uppercase letters.
fun_fact = fun_fact.upper()